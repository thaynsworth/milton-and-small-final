extends ../layouts/default

block content
	.container: .jumbotron
		img(src='/images/mands-wreath.jpg', width='160')
		h1 Welcome Brian!
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			p We have created a default Admin user for you with the email <strong>thomas.haynsworth@gmail.com</strong> and the password <strong>BSmalls2004!</strong>.
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
		hr
		p Let's do this!
